@model List<RuleRegulation>
@{
    ViewData["Title"] = "Index";
}


<section id="SualCavab">
    <div class="container">
        <div class="row">

            <div class="col-lg-12 col-md-12 col-sm-12 py-2">
                <h1 class="fw-bold">
                    Ən Çox Verilən Suallar
                </h1>
                @foreach (var rule in Model)
                {
                    <h5 class="fw-bold">@rule.Name</h5>
                    <p class="fw-light">@rule.Desc</p>

                }

            


            </div>
        </div>
    </div>
</section>